import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.citas.famosas.v6',
  appName: 'app-citas-famosas-v6',
  webDir: 'www'
};

export default config;
